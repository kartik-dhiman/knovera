# Tests

Automated tests are not added in this iteration.

Suggested initial tests:
- ingestion: parser + chunker unit tests
- query API: response shape with citation mode on/off
- integration: ingest sample PDF then ask deterministic query
